# GitHub

GitHub contribution heatmap and stats.

Install after this folder is a git repo (local path works; GitHub is optional):

```bash
omarchy plugin add /home/seb/projects/omarchy-plugin-github
```

Omarchy clones into `~/.config/omarchy/plugins/evo.github` using the manifest id.
